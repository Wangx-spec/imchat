可以。下面给你一版“**先基于你这次验证结论，再给改进实现**”的完整方案，按文件落地。

## 一、先基于验证结论（你日志已证明）

你这次 `tmp_rerank_diagnose` 的结果说明：

- `qwen_rerank_called=True` 且 `qwen_rerank_ok=True`  
  -> rerank 已接通，不是“没调用”问题。
- `top_titles_before_rerank` 里就没有目标文档（奥利奥、姜葱捞鸡）  
  -> 核心瓶颈在 **recall**，不是 rerank。
- `candidate_parent_count_before_trim=7`  
  -> 候选 parent 很少，召回池窄，rerank 无米可炊。

所以改进方向必须是：**先扩召回，再重排**。

---

## 二、完整改进方案（通用，不依赖“菜谱标题”）

目标：兼容后续任意文档类型（教程、规范、FAQ、手册），不仅是菜谱。

### 总流程（新）

1. `query expansion`（原 query + 轻改写 variants）  
2. 多路 `dense + sparse` 召回（对每个 variant）  
3. `direct lexical recall`（title/source/content 词面直达）  
4. 合并去重 -> parent 候选池  
5. 规则重排（你已有）  
6. `qwen-rerank` 模型重排  
7. top_k 截断 + 置信度信号回传

---

## 三、文件级实现

## 1) `src/rag/retriever.py`（核心改造）

### 1.1 Sparse 分词改为“通用 token 化”（不依赖 jieba）
把 BM25 初始化改成带 `preprocess_func`：

```python
self.bm25 = BM25Retriever.from_documents(
    children,
    preprocess_func=self._tokenize_for_sparse,
)
```

新增：

```python
def _tokenize_for_sparse(self, text: str) -> list[str]:
    t = (text or "").lower().strip()
    if not t:
        return []

    # 英文词
    words = re.findall(r"[a-z0-9_]+", t)

    # 中文二元切分（通用且无第三方依赖）
    zh = re.findall(r"[\u4e00-\u9fff]", t)
    bigrams = ["".join(zh[i:i+2]) for i in range(len(zh)-1)]

    # 原子中文字符也保留（短查询兜底）
    return words + zh + bigrams
```

---

### 1.2 Query expansion（通用）
新增：

```python
def _expand_queries(self, query: str) -> list[str]:
    q = (query or "").strip()
    if not q:
        return []

    variants = [q]
    q2 = q
    for pat in ["怎么做", "做法", "教程", "步骤", "如何", "需要什么"]:
        q2 = q2.replace(pat, "")
    q2 = re.sub(r"\s+", " ", q2).strip()
    if q2 and q2 != q:
        variants.append(q2)
        variants.append(f"{q2} 做法")
        variants.append(f"{q2} 教程")

    # 去重保序
    out, seen = [], set()
    for v in variants:
        if v and v not in seen:
            seen.add(v)
            out.append(v)
    return out[:4]
```

---

### 1.3 Direct lexical recall（不依赖标题）
新增：

```python
def _direct_parent_lexical_recall(self, query: str, limit: int) -> list[Document]:
    q = (query or "").strip().lower()
    if not q:
        return []

    hits: list[tuple[float, Document]] = []
    for p in self.parent_map.values():
        md = p.metadata or {}
        title = str(md.get("title", "")).lower()
        source = str(md.get("source", "")).lower()
        snippet = p.page_content[:1200].lower()

        score = 0.0
        if q in title:
            score += 3.0
        if q in source:
            score += 2.0
        if q in snippet:
            score += 1.2

        # token overlap
        q_tokens = set(self._tokenize_for_sparse(q))
        d_tokens = set(self._tokenize_for_sparse(title + " " + source + " " + snippet[:300]))
        overlap = len(q_tokens & d_tokens)
        score += min(overlap * 0.1, 1.5)

        if score > 0:
            hits.append((score, p))

    hits.sort(key=lambda x: x[0], reverse=True)
    return [x[1] for x in hits[:limit]]
```

---

### 1.4 多 query 召回并融合
在 `hybrid_search()` 里把单 query 改成 variants 循环：

- 每个 variant 跑 `vector_search` + `bm25_search`
- 汇总后再 `rrf_fuse`

建议 `retrieval_k_per_query`：
```python
k_per = max(8, retrieval_k // max(1, len(variants)))
```

---

### 1.5 候选池合并逻辑（关键）
`parents` 候选改为：
- `child_to_parent(fused_children, top_k=candidate_parent_k)`
- `direct_hits = _direct_parent_lexical_recall(query, limit=candidate_parent_k // 2)`
- 合并去重后再进 rerank

---

### 1.6 qwen-rerank 保留，但加可观测字段
在 debug 里补：

- `variant_queries`
- `direct_hit_count`
- `direct_hit_titles`
- `candidate_parent_count_before_trim`
- `qwen_rerank_called/qwen_rerank_ok/qwen_rerank_error`

---

## 2) `src/rag/service.py`（低置信度门控，防“编答案”）

在 `answer()` 中，拿 `ret.debug` 做轻门控（尤其 detail）：

```python
exact_hit = bool(ret.debug.get("exact_match_hit", False))
if route == "detail" and not exact_hit:
    # 返回“未精确命中”+来源，不进入“详细做法生成”
```

这样能避免“知识库没命中却输出完整配方”。

---

## 3) `src/config/settings.py` / `src/rag/types.py` / `src/rag/config.py`
你已在做 rerank 配置接线，继续确保三点：

- `Settings` 有 `rag_rerank_*` 并且真的传入 `return Settings(...)`
- `RAGConfig` 字段齐全
- `build_rag_config()` 完整映射

---

## 四、验证方案（你现有脚本直接升级）

在 `src/tests/rag/tmp_rerank_diagnose.py` 里多看这些字段：

- `variant_queries`
- `direct_hit_count`
- `direct_hit_titles`
- `top_titles_before_rerank`
- `top_titles`
- `qwen_rerank_ok/error`

### 验收标准
对这几条 query：
- `奥利奥冰淇淋怎么做`
- `姜葱捞鸡怎么做`
- `姜炒鸡怎么做`

至少满足：

1. `direct_hit_count > 0`（应出现对应文档）  
2. `qwen_rerank_ok=True`  
3. `top_titles` 含目标文档（Top1/Top3）  
4. 若仍未命中，返回“未精确命中”而不是伪造知识库步骤

---

## 五、参数建议（你当前阶段）

- `RAG_RETRIEVAL_K=30`（保留）
- `RAG_RRF_K=30~40`
- `RAG_RERANK_CANDIDATE_K=60`（从 40 提高）
- `RAG_RERANK_TOP_N=12`（从 8 提高）
- `RAG_RERANK_TIMEOUT_MS=3000~5000`

---

如果你需要，我可以下一条给你“`retriever.py` 的完整替换版（整文件）”，你可以直接覆盖，省去手动拼接。